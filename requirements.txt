# Required Python packages for Migration Analysis Tool

# GUI
tkinter-tools>=0.1.0; platform_system == "Linux"

# Excel/Spreadsheet handling
openpyxl>=3.1.0

# Image processing (for GUI)
Pillow>=10.0.0

# HTTP requests (for RTC REST API)
requests>=2.31.0

# AI Integration (Optional)
openai>=1.0.0  # For AzureOpenAI (Farm) Smart Merge + optional OpenAI GPT

# Windows-specific proxy authentication (Optional)
requests-ntlm>=1.2.0; platform_system == "Windows"

# Additional utilities
python-dateutil>=2.8.2

# Load .env files when running from terminal
python-dotenv>=1.0.0

# Credential management and security (Optional but recommended)
keyring>=24.0.0  # Secure credential storage using system keyring
cryptography>=41.0.0  # Encryption support for credentials (fallback if keyring unavailable)

